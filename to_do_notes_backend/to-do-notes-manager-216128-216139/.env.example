# Example environment variables for To-Do Notes backend
# If provided, will override application.properties server.port
SERVER_PORT=3001

# Spring profile (optional). Defaults to 'dev' for local.
SPRING_PROFILES_ACTIVE=dev
